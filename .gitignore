# Personal / Business — never publish
RSR/
Chronicle/
Research/
JobForcast/
Projects/
SaaS-Roadmap/
Agents/
Clive/

# Obsidian workspace files (machine-specific)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache

# Large binary files — images are referenced by URL not stored in repo
# (contributors use the URLs in frontmatter, not local copies)
Assets/Images/*.jpg
Assets/Images/*.png
Assets/Images/*.gif
Assets/Artifacts/*.jpg
Assets/Artifacts/*.png
Assets/Artifacts/*.gif

# OS files
.DS_Store
Thumbs.db

# Canvas files (Obsidian visual boards — personal)
*.canvas

# Financial and credential data — never publish
*.env
*.env.*
*credentials*
*secrets*
*tokens.json
*config.json
finances/
accounting/
quickbooks/
stripe-sync/
plaid-sync/
google-sync/
twilio-sync/

# Any file containing sensitive patterns (belt and suspenders)
# These patterns would only match filenames, not content:
*truist*
*invoice-data*
*client-list*
*payroll*
